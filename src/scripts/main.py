import os
import sys
import anthropic

def main():
    # what i believe needs to be done in this class:
    # 1. Load environment variables from .env
    # 2. Accept the transcript file path as an argument (until tool agent does this for us)
    # 3. Initialize the Anthropic client and pass it into the workflow
    # 4. Call start_workflow() from workflow.py
    # 5. Error handeling should be within this class
    # 6. Handle communication with output folder
    pass    